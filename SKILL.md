---
name: stamp-plate
description: 把用户给的一张风景图（照片或布艺拼贴作品）用**一条完整提示词**直接生成一枚真实感邮票（一图换一票）——暖象牙打孔齿边纸、贴画缘虚线针脚框、三段式传统排版、四轴质感（针法 / 做旧 / 齿孔 / 邮戳）全部由提示词驱动。当用户提到邮票、纪念票、齿孔、postage stamp、首日封、信销票、邮戳，或说「做成邮票样式」「把底层换成邮票」「做一个收藏票」时使用。关键约束：文字按「五区版式规格」各居其位绝不重叠、画面景物像素级不动、一次只喂一张参考图；主产线零 Python——纯提示词 + themes/*.json 参数预设，脚本全部为可选工具。
compatibility: 主产线零依赖零 Python（只需 image2 MCP edit_image）；可选脚本 fill_prompt / compose_sources / qc_stamp / stamp_kit 需 Python 3.10+ 与 Pillow + numpy。字体规则写在提示词里；本地精排字体缺失会显式报错（不静默降级）。
---

# 邮票画册（stamp-plate）

**一句话定位**：把**一张**用户图变成**一枚**完整邮票——**纯提示词一次成型**：
纸、齿孔、针脚框、全部排版文字、邮戳在一条提示词里生成；**整条工作流零
Python**（纯提示词 + themes/*.json 参数预设），scripts/ 下四脚本全部为可选工具。

## ★ 最终样式基准（用户拍板，2026-09-20）

基准图 = `demo/param-perf060.jpg`（随仓库分发；`examples/` 不入库，**勿引用其中文件**）。
图打不开时对齐以下 **7 条可核对特征**：

1. 画面 = 布艺拼贴/油画风景原样 + 贴画缘一圈深色虚线针脚框
2. 纸 = 暖象牙 laid 纸 + 打孔齿边（孔透桌面、孔内阴影、毛边缘）
3. 票微倾斜 ~1.5° + 软投影，浮在灰调桌面
4. 五区文字各居其位、零重叠（版式规格表见「强制输出契约」）
5. 墨色单一暖炭褐 `RGB(92,80,68)`，letterpress-flat，无阴影/描边字
6. 纸边上下 ≥10%（目标 10–13%）/ 左右 ≥8%（目标 8–10%）
7. **无**宽亚麻边框带、菱形角饰、花卉刺绣边（宽边框是可选支线，用户点名才走）

## 路由 / 模式

- **默认模式：纯提示词 / 提示词 + 参数预设（零 Python）**——读
  `references/stage2_template.txt` + `themes/*.json` 机械替换占位符组一条
  完整提示词（prompts.md 阶段 2），直接出整票；不装任何脚本也能走完。
- **特殊模式**：
  - 信销样：主模板第 6 段邮戳块（默认带上，用户要新票就删）
  - 刺绣宽边框支线：prompts.md 阶段 1c / 1c-2（用户点名才走）
  - **可选本地精排**：提示词路线反复失败或用户要求像素级排版精度时，
    才用附录的 stamp_kit.py（无字源票由 prompts.md 1e 出）
- **何时问**：①没有源图；②景色与海岸差很远且没给主题文案（标题/发行行/彩蛋
  要重写）；③用户点名要宽边框或本地精排。
- **何时不问**：主题默认海岸、面值 60¢、带邮戳信销样；直接按主模板编译出图。

## 加载参考

- **所有模式读**：本文件（路由 → 工作流 → 版式规格 → 质量门）。
- **出图前必读**：`references/prompts.md` **阶段 2（★一次成型主模板）** +
  阶段 3b（换景色彩蛋对照表）。
- **宽边框支线读**：prompts.md 阶段 1c / 1c-2 / 1d。
- **可选本地精排读**：prompts.md 阶段 1e（无字源票）+ 本文件附录。

## 工作流

1. **读取输入**：一张源图。没有 → 问。**多张** → 先拼成一张再喂
   （贴图/画图软件手动拼合；可选脚本 `scripts/compose_sources.py`）。
   图上有水印/文字 → 先裁掉（inpaint 会留痕，不修）。
2. **判断模式与主题**：默认海岸；换景从 `themes/*.json` 选一套参数预设
   （8 类景色全覆盖，见 prompts.md 3b 对照表；自定义景色仿照写一个 JSON）。
3. **组装提示词（零 Python）**：读 `references/stage2_template.txt`
   （**唯一真源**）+ 主题 JSON，按 prompts.md 阶段 2「占位符替换表」
   **机械替换** 11 个占位符（保真段 / 彩蛋段 / 邮戳段 / STRICT 段的
   现成文本预设也在那一节，逐字照抄），替换后通读确认**无 `{{` 残留**。
   **禁止手抄模板改写**（改错一处就成了自造词）。怕手误可用可选脚本
   `fill_prompt.py`（与手工替换逻辑同源）。
4. **生成**：`mcp__image2__edit_image`——只喂这一张参考图、`n=1`、
   `size 1536x1024`。524 等 120s 重试；下载 403 直接重试。
5. **质检（目检为主）**：按「质量门」清单逐项目检——尺寸 ±5% /
   纸边上下 ≥10% 硬·左右 ≥8% / 五区占用 / 右侧两段间隔 ≥5%H / 居中 ≤2–3% /
   齿孔行波动 / 纸纹 / 齿孔带入侵；**拼写放大 2× 逐字人工核对**
   （拼写没有像素判据，必须人眼过）。有 Python 环境可用可选脚本
   `scripts/qc_stamp.py 成片.png --bands <目录>` 出同一套判据的可复现
   PASS/FAIL 并导出 2× 字带。
6. **失败修正（决策树，局部修复优先）**：
   - **只坏局部**（某段字错 / 邮戳歪 / 一处越界）→ 不整票重掷：在提示词
     末尾**追加 STRICT 段现成文本**（prompts.md 阶段 2 预设）补强重出，
     或转可选本地精排只补排版。重出 = 全图重掷，实测「修居中 →
     拼写从对变错」——翻车点会漂移。
   - **必须重出** → 先写明这次换掉**哪几个关键词**（其余不动），出图后
     **强制重跑全量质检**（含上次已过的项），不是只查上次失败项。
   - **两轮全量质检仍失败** → 转可选本地精排（附录）。
7. **返回输出**：成片 + 所用提示词要点记录（按「输出格式」）。

## 输入边界与保留

- **图片角色**：输入图 = **画面内容源**，构图/笔触/布片就是成品的画面本体。
- **单图限制（刻意取舍）**：image2 喂两张（内容 + 风格）稳定返回
  `upstream_error`——**永远只喂一张**。多张素材 → 先拼合成一张再喂
  （手动拼合即可，可选脚本 `compose_sources.py`）；**风格参考一律写成
  文字**（`oil painting style` 等逐条写进提示词），不找第二张风格图。
- **照片输入分支**：源图是**照片**（非布艺作品）时，保真段必须换 P 版
  （`PRINTED PHOTOGRAPH` 措辞 + `do NOT turn it into a fabric collage`）+
  `{{ARTWORK_NOUN}}` 用 `photograph`——默认模板里 `cloth patches /
  embroidery` 的措辞会诱导模型把照片布艺化；彩蛋同步选 photo 态
  （移到纸边不进画面，或干脆 photo_clean 无彩蛋）。三段预设全文见
  prompts.md 阶段 2。
- **保留等级**：L0 景物像素级不动——`CRITICAL: do not move, redraw or restyle
  the landscape, the cloth patches or the threads`（一次成型更要用强措辞，
  否则模型趁排版顺手重画画面）；L1 结构保留——遮罩类支线作业时保护区
  diff ≈ 0；L2 纸面可重绘。
- **禁止复制**：输入图自带文字/水印/签名一律裁掉不修。
- **文字串只有两种来源（「禁止自造词」的判据）**：
  - **A 固定串**：`side_left / engraver / postmark_name / postmark_date / year`
    ——模板自带的虚构专名（LITO 系列、`ENGRAVED BY A. MOREL`、
    `14 - IX - 2026`）**视为已授权**，随主题 JSON 原样使用：不随景色改写、
    不改动拼写；日期间隔点一律连字符 `-`（写 `·` 会变方框）；
  - **B 专名位**：`title / series / value / side_right / eggs(3 条)`
    ——随景色重写，重写后同样加引号锁死。
  提示词里**每个字符串必须逐字来自 A∪B 的引号原串**，这就是自造词的
  客观判据（不再有「模板自带专名 vs 禁止编词」的矛盾）。

## 强制输出契约

- **比例 / 尺寸**：`1536x1024`，**验收容差 ±5%**（实测同一参数会拿到 3 种
  尺寸，卡死整数必假 FAIL）；纸边目标值上下 **10–13%**、左右 **8–10%**，
  验收线分开：上下是**硬约束 ≥10%**（文字必须放得下），左右是**下限 ≥8%**。
- **必须出现**：齿孔 = 打孔物理过程（`punched clean through` + 孔内阴影 +
  纤维毛边，只写 perforated edge 会得到数字模切）；贴画缘虚线针脚框；
  微倾斜 1.5° + 软投影；五区文字齐全（下表）；面值**必须带单位**（`60¢`）；
  墨色单一暖炭褐 `RGB(92,80,68)`，letterpress-flat。
- **禁止出现**：文字重叠 / 文字越界压画面或齿孔 / 自造词 / 拼写错误 /
  第二墨色 / 邮戳以外的邮政元素 / 灰团假齿孔。
- **★ 五区版式规格表**（提示词排版段的依据，也是质检的验收线）：

| 区域 | 内容 | 位置与限长 | 字号（占画幅高） | 方向 |
|---|---|---|---|---|
| 顶部 | 标题 `THE BLUE BAY` | 顶纸边**水平居中**，可全长 | ~3.5% | 横排，宽字距 |
| 底部 | `POSTA - COASTAL SERIES` + `60¢` + `2026` | 底纸边**同一行水平居中**，三元素间隔均匀，面值放大约 1.4× | ~2.2%（面值 ~3%） | 横排 |
| 左 | logo + `LANDSCAPE ISSUE - TEXTILE STUDY` | logo 占左上 1/4；文字**垂直居中、限中部 50%** | 文字 ~1.8% | 自下而上 |
| 右·上段 | 署名 `ENGRAVED BY A. MOREL` | **仅右上 1/4 区** | ~1.6% | 自上而下 |
| 右·中段 | `IMPRESSIONIST - POST-IMPRESSIONIST` | **仅中部 50% 区**，与上段间空隙 ≥5%H | ~1.8% | 自上而下 |
| 右下 | 圆形邮戳（可选） | 半透明，只压画面右下角与其下方纸面，**不碰任何文字** | — | 环形 + 波浪线 |

> 右侧两段竖排是叠字事故区：必须写「two separate zones … a clear gap of at
> least 5 percent of the image height separates them; their zones never touch」。

## 决策清单 / Prompt 结构（五段式）

每次出图从 prompts.md **阶段 2 主模板**取用，五段拼装：

1. **源图保真**——景物不动段（L0 原文）。
2. **邮票方向**——纸 + 打孔齿边物理过程 + 倾斜投影 + 纸边宽度。
3. **材质 / 图形**——纸纹纤维、虚线针脚框、彩蛋（显式极小尺寸）。
4. **★ 排版规格**——五区版式规格表逐条翻译成提示词（每区：内容字符串加引号 +
   位置 + 限长 + 字号比例 + 方向 + `never overlapping`）。
5. **禁止项**——`no overlapping or colliding text` 打头 + AVOID 列表。

**失败修正（每类一次到位）**：
- 右侧竖排叠字 → 重申两区限长 + `a clear gap of at least 5 percent` +
  字号降到 1.6% 以下
- 间隔点错形（变方框）→ 字符串里改用连字符 `-`（如 `POSTA - COASTAL SERIES`）
- 模型自造词/拼错 → 确认字符串已加引号逐字给出；仍错 → 在排版段重抄一遍全句
- 齿孔成灰团 → 重申 `punched clean through, each hole showing the grey tabletop
  through it`（孔要透）
- 景物被重画/加框 → 保真段提到提示词最前 + `copy the artwork pixel for pixel`
- 纸边过窄文字挤压 → 明确 `paper margins of 12 percent top and bottom, 9 percent
  left and right`

## 质量门

- [ ] **五区文字各居其位，零重叠**（右侧署名/风格线两区间隔清晰）
- [ ] **拼写零错误**（放大逐字核对，字符串全部来自规格表）
- [ ] 面值带单位；底部三元素同一行、间隔均匀
- [ ] 墨色单一，无阴影字/描边字/渐变字
- [ ] 齿孔是打孔（孔透出桌面、有阴影与毛边），不是印刷或灰团
- [ ] 虚线针脚框贴画缘；无宽亚麻边框带/角饰（除非用户点名支线）
- [ ] 景物与来源图一致，未被重画或加框
- [ ] 纸边够宽（上下 10–13% / 左右 8–10%），文字未越界压画或压齿孔
- [ ] 邮戳（若有）只压画面右下与纸面，未碰任何文字
- [ ] 纸张有纤维感，微倾斜 + 软投影在
- 质检 FAIL（目检任一项，或可选 `qc_stamp.py` FAIL）→ 按工作流
  「失败修正决策树」走：局部修复优先；重出后**重跑全量质检**。
  两轮仍不过 → 转可选本地精排（附录）。
- 「齿孔带入侵·左/右」FAIL 且走的是精排路径 → 重跑加 `--perf-clear 0.042`
  （竖排外轨给齿孔让位；默认 perf_clear=0 时外轨距纸边仅 ~20px）。

## 输出格式

```markdown
## 成片
- 文件：<out>.png（1536×1024，一图换一票）
- 版式：一次成型（基准五区版式）；主题：海岸 / 自定义
- 文字：标题 / 发行行 / 面值 / 年份 / 署名（全部加引号字符串）
- 邮戳：有（信销样）/ 无（新票）

## 质检
- 五区文字：零重叠 ✅/❌（❌ 逐条说明）
- 拼写：逐字核对结果
- 齿孔/纸边/景物保真：✅/❌
- 修正轮次：n（提示词补强点）
```

## 附录 · 可选脚本工具箱（主产线一个都不用）

四个脚本全部是**可选工具**，一行定位：`fill_prompt.py` = 批量/怕手误时
替代手工替换占位符；`compose_sources.py` = 多图拼合成一张；
`qc_stamp.py` = 质量门判据的可复现 PASS/FAIL + 导出 2× 字带；
`stamp_kit.py` = 像素级本地精排（下文）。

仅当提示词路线反复失败或用户要求像素级排版精度时使用精排：
先按 prompts.md **阶段 1e** 出**无字**源票（纸面 COMPLETELY BLANK），
再跑 `scripts/stamp_kit.py` 本地排版（确定性、可复现）：

```bash
python scripts/stamp_kit.py --src 源票.png --out 成片.png \
    --stitch .7 --age .25 --perf .95 --cancel 0 \
    --margin-v 0.12 --margin-h 0.10        # 与出图纸边一致
# 紧凑纸边（6–9%）三件套：--perf-clear 0.042 --perf-frame "fx0,fy0,fx1,fy1" --no-strip
# 五档预设：--preset 全新发行/标准/信销票/古董藏票/密绣精工（英文 Mint/Standard/
#   Used/Antique/Dense 均可）；矩阵对比：--sheet
```

- 四轴：`--stitch` 针法 / `--age` 做旧 / `--perf` 齿孔完整度（只做破坏）/
  `--cancel` 邮戳。
- **主题**：`--theme <键|JSON路径>` 从 `themes/*.json` 读（9 套：coastal /
  coastal_cn / hilltown / avenue / meadow / snow / lake / desert / garden）；
  自定义景色仿照写 JSON 即可，脚本不写死主题。
- **字体是显式失败，绝不静默降级**：找不到候选字体或缺字形 →
  `FontNotFoundError` 附三平台安装指引。旧行为（静默退回位图字体）会整票
  质感作废且无提示，已移除。
- **纸面几何自动量测**：`render()` 先量纸面、再量内容，按实测边距排版
  （票浮在桌面带倾斜时画布边 ≠ 纸边）；检测不可信时 `--paper-frame
  fx0,fy0,fx1,fy1` 显式给框——可控 > 聪明。
- 位置用显式参数**不要自动检测**（四代检测算法全败于米色亚麻与齿孔干扰）；
  竖排双轨道宽度预算：净空带 ≥ 两条轨道（各约 `字号×1.45`）。
- 纸张验收（高频 std ≥6、齿孔行波动 ≥18）**已算法化**：`scripts/qc_stamp.py`
  直接对成片出 PASS/FAIL，不用手量。
- 2026-09-20 修复的精排 bug：①typeset 从画布边量纸边 → 改按实测纸面；
  ②layer_stitch 补框横穿文字 → 最长连续段检测；③字号只看上纸边致面值
  溢出 → 上下双边约束；④右侧竖排双轨道把左侧绝对坐标当距离用 → 右轨
  整体内移压画面，已改镜像距离。

## 文件

```
stamp-plate/
├── SKILL.md
├── themes/                   主题库（9 套 JSON = 3b 对照表 8 类景色 + 中文面值版）
│   └── coastal.json … garden.json
├── scripts/                  可选工具箱（主产线零 Python，一个都不用）
│   ├── fill_prompt.py        批量/怕手误时替代手工替换（模板+JSON→整条提示词）
│   ├── compose_sources.py    多图预合成一张（横向/纵向/2×2）
│   ├── qc_stamp.py           可复现质检（质量门判据算法化）
│   └── stamp_kit.py          可选本地精排：四轴 + 主题排版
├── references/
│   ├── prompts.md            提示词模板库（★2 用法与判据 / 1 基础 / 1b /
│   │                         1c·1c-2 刺绣边框支线 / 1d 边框精化 / 1e 无字源票 /
│   │                         3b 彩蛋）
│   └── stage2_template.txt   ★一次成型模板唯一真源（占位符版）
└── demo/
    └── param-perf060.jpg     ★最终样式基准图（examples/ 不入库）
```
